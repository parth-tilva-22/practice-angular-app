export interface Task{
    title: string;
    description: string;
    dueDate: string;
    isComplete: boolean;
    priority: number;
    // constructor(title: string, description: string, dueDate: string, isComplete: boolean, priority: number ){
    //     this.title=title;
    //     this.description = description;
    //     this.dueDate = dueDate;
    //     this.priority = priority;
    //     this.isComplete = isComplete;
    // }
}